diff --git a/thirdparty/dependencies.cmake b/thirdparty/dependencies.cmake
index 95b97fcb77ac128bea42d9a64a9a9352cc72e3fe..591929e4775b6906ca596d553f62ce70490e7c62 100644
--- a/thirdparty/dependencies.cmake
+++ b/thirdparty/dependencies.cmake
@@ -72,7 +72,7 @@ endif()
 # LevelZero
 #
 
-if(ENABLE_INTEL_GPU OR ENABLE_INTEL_NPU)
+if(ENABLE_INTEL_NPU OR (ENABLE_INTEL_GPU AND GPU_RT_TYPE STREQUAL "L0"))
     if(ENABLE_SYSTEM_LEVEL_ZERO)
         pkg_search_module(level_zero QUIET
                           IMPORTED_TARGET
diff --git a/thirdparty/level_zero/CMakeLists.txt b/thirdparty/level_zero/CMakeLists.txt
index 62c2d179b2c3888a8f7057d6f49f26fcae0111ec..6782deae0d2352d2625214ce0c4f6b6c47fdc47e 100644
--- a/thirdparty/level_zero/CMakeLists.txt
+++ b/thirdparty/level_zero/CMakeLists.txt
@@ -2,6 +2,14 @@
 # SPDX-License-Identifier: Apache-2.0
 #
 
+# Level Zero is only needed when:
+#   - NPU plugin is enabled (ENABLE_INTEL_NPU), OR
+#   - GPU plugin uses Level Zero runtime (ENABLE_INTEL_GPU AND GPU_RT_TYPE=L0)
+# Skip the entire module otherwise.
+if(NOT ENABLE_INTEL_NPU AND NOT (ENABLE_INTEL_GPU AND GPU_RT_TYPE STREQUAL "L0"))
+    return()
+endif()
+
 set(BUILD_SHARED_LIBS OFF)